#!/usr/bin/env bash
set -euo pipefail

export PYTHONUNBUFFERED=1
export PATH="/code/python-packages/bin:$PATH"
export PYTHONPATH="/code/python-packages:${PYTHONPATH:-}"

PORT="${FC_SERVER_PORT:-9000}"
exec python3 -m uvicorn app:app --host 0.0.0.0 --port "$PORT"
